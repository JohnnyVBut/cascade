# Official Caddy Alpine image — no custom build needed.
# Rate limiting via caddy-ratelimit plugin removed: requires xcaddy compilation
# (~1.5GB RAM, 15+ min on small VPS). Protection is provided by ADMIN_PATH
# obscurity + bcrypt password + TOTP 2FA instead.
FROM caddy:alpine
