node_modules/

# Vite build output is regenerated by `npm run build` (and by the Dockerfile
# frontend-builder stage). Everything under dist/ is ignored EXCEPT the tracked
# placeholder index.html, which must exist so `go:embed all:ui2/dist` never
# fails on a fresh clone that hasn't run npm.
dist/*
!dist/index.html
